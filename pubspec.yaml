name: tableronde_app
description: Écosystème communautaire complet - Social, Finance, Éducation, Jeux
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI & Theming
  google_fonts: ^8.0.0
  
  # Icons
  cupertino_icons: ^1.0.2
  image_picker: ^1.1.2

  #File Picker
  file_picker: ^10.3.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  # Assets
  assets:
    - assets/fonts/
    - assets/images/
  #   - assets/avatars/
